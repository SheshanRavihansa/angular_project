export interface Product {
  id: number;
  name: string;
  description: string;
  category: string;
  expiredDate: string;
  manufacturedDate: string;
  batchNumber: string;
  quantity: number;
  unitPrice: number;
  createdDate: string;
}
